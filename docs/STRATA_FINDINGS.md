# M2 — Strata: Findings & Discussion → moved

> **Superseded.** M2 was reworked on the certified Gaussian-copula map (full-N, cohort-weighted). The
> canonical M2 findings now live in **[STRATA_OOP_FINDINGS.md](STRATA_OOP_FINDINGS.md)** (atlas:
> **[STRATA_OOP_ATLAS.md](STRATA_OOP_ATLAS.md)**). This file is kept only as a redirect so existing links
> resolve. Updated 2026-06-22.

## What changed (native map → copula map)

The earlier native-map M2 reported **8 archetypes + a K=4 tessellation** as the lead. On the better-converged
copula map the picture is sharper and more honest:

- **The space is a continuum** — confirmed, now with a single-Gaussian falsification null (best-K separation
  is statistically identical to a structureless blob; silhouette z ≈ 0.1).
- **The load-bearing objects are granularity-free:** the continuous 9-dim coordinates (+ uncertainty) and a
  **stable A = 4 archetype simplex** (the native A = 8 does **not** reproduce under the copula's honest
  explicit-axis uncertainty).
- **The tessellation is a coarse convention with no privileged K.** A nested **K-family (2/3/4)** is exported;
  the *operative* K is deferred to M4/M5 incremental validity. (K = 2 is only the M3-contract default;
  K = 3 is BIC-best; finer K progressively captures the severity/biology gradient K = 2 discards.)

See **[STRATA_OOP_FINDINGS.md](STRATA_OOP_FINDINGS.md)** for the full record and the per-K decision menu.
The native-map development detail remains in [STRATA_RESULTS.md](STRATA_RESULTS.md) (provenance) and in git
history.
