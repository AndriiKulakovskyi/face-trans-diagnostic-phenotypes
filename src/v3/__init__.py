"""FACE V3 — precision-psychiatry stratification & decision-modeling.

A self-contained package. `v3.data` is the harmonization + observed-data loading layer (no naive
imputation); modeling sub-packages (bayesian / strata / prognosis / …) are added as the pipeline grows.
Plan of record: docs/V3_PLAN.md.
"""
