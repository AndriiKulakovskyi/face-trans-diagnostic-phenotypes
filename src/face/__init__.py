"""FACE — clinical-biological transdiagnostic stratification (BP · SZ · DR).

A self-contained package. `face.data` is the harmonization + observed-data loading layer (no naive
imputation); modeling sub-packages (`face.priors`, `face.measurement`, …) build the measurement
model. Methods of record: docs/MEASUREMENT_MODEL.md.
"""
